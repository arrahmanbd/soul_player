import 'package:flutter/material.dart';

const primary = Colors.blue;
const deepDark = Color(0xFF0C0C0E);
const secondaryDark = Color(0xFF1C1C20);
//text
const wh = Color(0xC7FFFFFF);

/// Dynamic Color Selector
/// Select Accent and Primary SChema
const ColorScheme colorScheme1 = ColorScheme.light(
  primary: Colors.blue,
  secondary: Colors.blueAccent,
  onPrimary: Colors.white,
  onSecondary: Colors.black,
);

const ColorScheme colorScheme2 = ColorScheme.light(
  primary: Colors.red,
  secondary: Colors.redAccent,
  onPrimary: Colors.white,
  onSecondary: Colors.black,
);

const ColorScheme colorScheme3 = ColorScheme.light(
  primary: Colors.green,
  secondary: Colors.greenAccent,
  onPrimary: Colors.white,
  onSecondary: Colors.black,
);

const ColorScheme colorScheme4 = ColorScheme.light(
  primary: Colors.purple,
  secondary: Colors.purpleAccent,
  onPrimary: Colors.white,
  onSecondary: Colors.black,
);
